PORT=8787
GH_OAUTH_CLIENT_ID=
GH_OAUTH_CLIENT_SECRET=
# App 回跳地址，授权完成后会重定向到这里（默认 githubmobile://auth）
MOBILE_APP_CALLBACK_URI=githubmobile://auth
# 可选：限制 code exchange 允许的 redirect_uri（逗号分隔）
# 为空时自动允许 MOBILE_APP_CALLBACK_URI 以及当前服务的 /oauth/github/callback
OAUTH_REDIRECT_URI_ALLOWLIST=
